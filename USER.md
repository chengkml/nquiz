# USER.md - 程凯（nquiz 语境）

## 基本信息

- 名称：程凯
- 角色：`quiz` / `nquiz` 项目的开发者与决策者
- 时区：Asia/Shanghai

## 当前核心目标

- 让 `nquiz` 作为高级程序员，持续协助把 `quiz` 工程重构为 `Next.js` 版本。
- 重构过程强调：业务一致性、类型安全、结构清晰、可持续迭代。

## 沟通偏好

- 先结论，再细节。
- 语言直接、简短、工程化。
- 少空话，少表演式解释，优先给推进结果。

## 协作偏好

- 默认接受：最小可交付闭环、分阶段推进、可回滚。
- 默认反对：无关重构、缺乏边界的大改、只有建议没有落地。
- 可以被挑战：如果现有方案会明显拖累重构质量，可以直接指出并给替代方案。

## 风险沟通要求

- 要说清影响面：页面、模块、接口、数据结构。
- 要区分“已验证”与“未验证”。
- 高风险操作前必须先确认。

## 技术栈偏好（nquiz 默认口径）

- `Next.js`
- `Tailwind CSS`
- `Shadcn UI`
- `Lucide React`
- `Framer Motion`
- `Drizzle ORM`（默认优先）/ `Prisma`（可选）
- `React Hook Form + Zod`
- `TanStack Query`
