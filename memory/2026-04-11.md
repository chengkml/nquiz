# 2026-04-11

## 部署与交付
- 已参考 `quiz/.github/workflows/main.yml`，在 `nquiz` 新增 `.github/workflows/main.yml` 自动部署工作流。
- 当前工作流已改为“GitHub Actions 构建 standalone 产物并上传服务器”口径：runner 执行 `npm ci` 与 `npm run build`，把 `public + .next/static` 组装进 `.next/standalone` 后打包上传服务器，服务端只负责解压、切换 `current` 软链并重启 `systemd` 服务。
- 已在工作流文件顶部注明必填 `GitHub Secrets`（`SERVER_HOST`、`SERVER_USER`、`SERVER_SSH_KEY`）和可选 `GitHub Variables`（`DEPLOY_PATH`、`SYSTEMD_SERVICE`、`KEEP_RELEASES`），避免后续接入时再反复口头同步。
- 已把 `next.config.ts` 切到 `output: "standalone"`，并把 `package.json` 的生产启动命令调整为 `node .next/standalone/server.js`，与 Next.js 16 的 standalone 部署口径对齐。
- 已新增 `deploy/nquiz.service.example`，作为服务器首次部署时的 `systemd` 模板参考。
- 当前方案显式收敛为“传统服务器 + Nginx 反向代理 + Next.js standalone Node 服务”，未引入 Docker、Vercel。

## 技能新增
- 已新增项目内技能 `skills/nquiz-queue-dev`，用于串联 `requirement-query` 与 `cm-nquiz-requirement-develop`。
- 技能固定流程：先查询 `projectName=nquiz` 且 `status=OPEN` 的待处理需求；若无待处理项则立即停止；若有则只启动 1 条需求进入真实开发闭环。
- 技能正文已显式兼容用户口误：若请求写成 `cm-quiz-requirement-develop`，实际映射到已安装技能 `cm-nquiz-requirement-develop`。
- 已生成 `skills/nquiz-queue-dev/agents/openai.yaml`，并通过 `python3 /root/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/nquiz-queue-dev` 校验。
- 额外记录：仓库 `.agents` 目录是只读挂载，无法直接在 `.agents/skills` 下新增技能目录；因此本次技能先落在仓库可写目录 `skills/`。
- 已按新要求升级 `skills/nquiz-queue-dev`：把 `cm-nquiz-requirement-develop` 的开发闭环逻辑直接并入技能正文，并改成队列循环模式。
- 新口径要求：先查 `nquiz` 的 `OPEN` 队列，再持续执行 `start -> 开发 -> progress -> complete -> 再查队列`，直到 `OPEN` 数量为 `0` 才允许停止。
- 本次已同步更新 `skills/nquiz-queue-dev/agents/openai.yaml`，使触发文案从“只跑一条”改成“循环直到队列清空”。
- 进一步修正 `skills/nquiz-queue-dev` 停止条件：新增“Mandatory Pre-Final Gate / Hard Stop Policy”，要求在任何终态回复前必须重新查询 `OPEN` 队列并确认 `activeRequirementId=null`、`OPEN=0`；完成单条需求后只允许发中间进度，不允许直接收尾。
- 已新增 `skills/nquiz-queue-dev/scripts/pre_final_gate.py`，把“能否终止 queue run”从纯提示词约束升级为可执行门禁；当前实测在 `OPEN=55` 场景下会正确返回非零并拒绝收尾。

## 需求开发
- 已完成需求 `294962874827149258`（[nquiz迁移] 通知异常日志页）首版真实开发并准备回写。
- 本次在 `nquiz` 中新增 `/notifications/exception-logs` 与最小服务端闭环，形成“异常日志查询 + 结构化展示 + 详情查看 + 单条重试”的首版闭环。
- 已落地内容：
  - `src/app/notifications/exception-logs/page.tsx`：新增 App Router 页面入口。
  - `src/features/notifications/exception-logs/{types,schema,api/client,exception-log-page}.tsx`：完成类型、Zod 筛选约束、typed fetch client、URL 化筛选、详情抽屉、确认重试交互。
  - `src/app/api/notifications/exception-logs/route.ts`、`src/app/api/notifications/exception-logs/[id]/retry/route.ts`：补齐查询与单条重试 route handler。
  - `src/lib/notifications/exception-logs/mock-store.ts`：补齐服务端 mock store，统一负责 payload 解析、结构化 DTO 映射、不可重试原因和重试结果。
  - `src/lib/query/query-keys.ts`、`src/components/home/hero.tsx`：补齐通知异常日志 query key 与首页入口。
- 本次与旧 quiz 的关键差异：
  - 不再在前端直接 `JSON.parse messageContent`，改为服务端统一映射 `senderId / recipient / title / rawPayload / retryable` 等稳定字段。
  - 首版增加详情抽屉，支持查看原始 payload、完整异常信息和最近重试结果。
  - 不可重试日志会直接展示阻断原因；重试成功后记录从 ERROR 列表中移出，更贴近故障排障台账语义。
- 已验证项：
  - `npx eslint src/app/notifications/exception-logs/page.tsx src/app/api/notifications/exception-logs/route.ts src/app/api/notifications/exception-logs/[id]/retry/route.ts src/features/notifications/exception-logs/exception-log-page.tsx src/features/notifications/exception-logs/api/client.ts src/features/notifications/exception-logs/schema.ts src/features/notifications/exception-logs/types.ts src/lib/notifications/exception-logs/mock-store.ts src/lib/query/query-keys.ts src/components/home/hero.tsx` 通过。
  - `npm run build` 通过，产物中已包含 `/notifications/exception-logs`、`/api/notifications/exception-logs` 与 `/api/notifications/exception-logs/[id]/retry`。
- 已完成需求 `294962874827149261`（[nquiz迁移] 独立对话页）首版真实开发并准备回写。
- 本次在 `nquiz` 中新增 `/chat` 与 `/chat/[sessionId]`，形成“用户私有会话 + 默认知识问答 + 模型切换 + 流式回复 + 参考来源展示”的首版闭环。
- 已落地内容：
  - `src/app/chat/page.tsx`、`src/app/chat/[sessionId]/page.tsx`：新增 App Router 路由入口与会话深链页。
  - `src/features/chat/{types,schema,mock-service,use-chat-stream,chat-markdown,chat-workbench-page}.tsx`：完成 chat 域类型、RHF+Zod 输入约束、localStorage mock store、流式输出 hook、轻量 Markdown 渲染和独立工作台页面。
  - `src/lib/query/query-keys.ts`、`src/components/home/hero.tsx`：补齐 chat query keys 与首页入口。
- 本次与旧 quiz 的关键差异：
  - 保留“默认按知识范围问答”语义，不把该页扩成无限制自由聊天。
  - 显式区分“全部可访问知识集”和“单知识集定向问答”，并在切换范围时直接重置为新会话。
  - 增加 `/chat/[sessionId]` 深链，支持直接打开历史会话。
  - 前端按 `sessions/messages/models/scopes + streaming state` 拆开，不再延续旧页那种把所有状态挤在单组件里的实现。
- 依赖与边界：
  - 当前可访问知识集与参考来源直接复用 `PersonalKnowledge` mock store，因此两页的数据语义保持一致。
  - 当前仍未接真实聊天后端、真实 SSE、真实 LLM 模型中心与真实向量检索；引用与回答由本地 mock 规则生成。
- 已验证项：
  - `npx eslint src/app/chat/page.tsx src/app/chat/[sessionId]/page.tsx src/features/chat/chat-workbench-page.tsx src/features/chat/chat-markdown.tsx src/features/chat/mock-service.ts src/features/chat/schema.ts src/features/chat/types.ts src/features/chat/use-chat-stream.ts src/lib/query/query-keys.ts src/components/home/hero.tsx` 通过。
  - `npm run build` 通过，产物中已包含 `/chat` 与 `/chat/[sessionId]` 路由。
- 已完成需求 `294962874827149279`（[nquiz迁移] 个人知识页）真实开发，并通过 `cm-nquiz-requirement-develop` 回写 `COMPLETED`。
- 本次落地结果：
  - 新增 `src/app/knowledge/personal/page.tsx` 与 `src/features/personal-knowledge/{types,schema,mock-service,personal-knowledge-page}.tsx`，完成个人知识工作台首版闭环。
  - 页面核心覆盖：我创建的 / 共享可访问知识集导航、知识集 CRUD、Markdown/文件来源管理、处理状态轮询可见、知识集维度问答、首页入口跳转。
  - 实现语义明确按需求 descr 收敛：不复刻旧 `PersonalKnowledge` 空壳页，不保留脱离上下文的静态输入框，把问答能力绑定到当前知识集。
  - 权限边界首版显式收敛：共享知识集只读；仅自建知识集允许维护来源；问答以当前知识集启用态为前提。
- 为通过本次 `complete` 门禁，额外修复了多处现存构建阻断：
  - 修复 `wrong-question`、`hot-search`、`knowledge-mastery`、`question-bank-statistics` 等页面里的语法/导入残留。
  - 统一补齐 `role / subject / wxapp / user-management / wrong-question / hot-search / price-monitor / personal-knowledge` 等模块的 RHF + Zod input/output 泛型口径，避免 `next build` 在完整 typecheck 下失败。
  - 补齐 `queryKeys` 中缺失的 `knowledgeMasteryDashboard`、`users`、`personalKnowledge` 等键，并为使用 `useSearchParams()` 的路由入口增加 `Suspense` 包裹。
- 已验证项：
  - `npx eslint src/app/knowledge/personal/page.tsx src/features/personal-knowledge/personal-knowledge-page.tsx src/features/personal-knowledge/mock-service.ts src/features/personal-knowledge/schema.ts src/features/personal-knowledge/types.ts src/lib/query/query-keys.ts src/components/home/hero.tsx` 通过。
  - `npm run build` 通过，产物中已包含 `/knowledge/personal` 静态路由。

## 技能修正
- 已修正 `/root/.agents/skills/cm-nquiz-requirement-develop` 的工作流与脚本实现，不再使用“`action=full` 自动开发闭环 + auto-query/checkpoint”的旧逻辑。
- 新逻辑改为阶段化四步：`query/start/progress/complete`；脚本只负责 JWT 链路、需求状态流转、本地 active requirement 状态记录，以及 `complete` 前的源码改动 + `npm run build` 门禁。
- 已修复旧脚本从技能目录向上探测 repo root 的缺陷；新版本默认从当前工作目录向上探测 `nquiz` 仓库，或显式使用 `--repo-root`。
- 新版本默认一次只允许 1 条 active requirement，并将本地状态文件改到 `/tmp/cm-nquiz-requirement-develop-state.json`，避免写技能目录 runtime 造成权限问题。
- 已将该 skill 的“待开发需求”查询口径进一步收敛为仅 `OPEN`；默认不再查询或允许传入 `IN_PROGRESS`。
- 已验证项：工作区副本与已安装脚本均可正常输出 `--help`；脚本通过 `python3 -m py_compile` 静态编译校验；参数错误场景能正确返回 `step=validate`。
- 未验证项：未实际调用 `https://www.quizck.cn` 的登录/JWT/需求接口，未执行真实 `query/start/progress/complete` 线上链路。

## 需求分析
- 完成需求 `294962874827149207`《[nquiz迁移] ApiTester》分析并通过 `nquiz-requirement-analyze` 回写。
- 基于真实代码确认 quiz 旧版 `ApiTester` 核心能力：HTTP 调试、自动 Bearer Token、Swagger/OpenAPI 端点发现、cURL 导出、datasetContext 注入（编排调试增强）。
- 明确 nquiz 当前仅有 Next.js 基础骨架，结论为“从 0 到 1 的迁移分析”，非已有页面修订。
- 分析建议：首版按 `App Router + RHF + Zod + TanStack Query` 重构，优先保留浏览器直发模式，datasetContext 下沉为高级调试区，OpenAPI 地址改为配置驱动。
- 回写后需求状态已从 `PENDING_ANALYSIS` 进入 `PENDING_REVIEW`。

- 已完成需求 `294962874827149162`（[nquiz迁移] 错题本页）分析准备：基于 `quiz` 真实代码阅读确认该页面本质是“当前登录用户自己的错题记录台账”，而不是全局题库管理页。
- 关键证据：`frontend/src/pages/WrongQuestion/index.tsx`、`frontend/src/pages/WrongQuestion/api/index.ts`、`backend/src/main/java/com/ck/quiz/wrongquestion/service/impl/WrongQuestionServiceImpl.java`、`backend/src/main/java/com/ck/quiz/ocr/controller/OcrController.java`、`backend/src/main/java/com/ck/quiz/file/controller/FileController.java`。
- 已确认核心能力：列表筛选、增删改、学科/分类依赖、原图上传、OCR SSE 识别、识别文本回填。
- 已识别主要风险：菜单权限与个人数据语义冲突、文件先上传后取消导致孤儿文件、OCR 强依赖视觉模型与提示词模板、nquiz 新表命名需遵守 `quiz_` 前缀约定。
- 已完成需求 `294962874827149189`（[nquiz迁移] SystemParamManagement）分析并回写：基于 `quiz` 真实代码确认该页是运行时配置中心，不是普通 CRUD 页；已识别其直接支撑邮件、文件存储、百度网盘、热搜等模块。
- 关键证据：`quiz/frontend/src/pages/SystemParam/index.tsx`、`quiz/frontend/src/pages/SystemParam/api.ts`、`quiz/backend/src/main/java/com/ck/quiz/config/entity/SystemParam.java`、`quiz/backend/src/main/java/com/ck/quiz/config/service/impl/SystemParamServiceImpl.java`、`quiz/backend/src/main/java/com/ck/quiz/config/MailConfig.java`、`quiz/backend/src/main/java/com/ck/quiz/file/config/FileStorageConfig.java`、`quiz/backend/src/main/java/com/ck/quiz/baidupan/service/impl/BaiduPanServiceImpl.java`、`quiz/backend/src/main/java/com/ck/quiz/hotsearch/service/impl/HotSearchRuntimeConfigServiceImpl.java`。
- 本次分析结论：`nquiz` 应迁移为 `/settings/system-params` 语义化设置页，底层建议新增 `quiz_system_param` 表并保留 `paramName` 运行时 key 语义，首批覆盖参数 CRUD、分类筛选、只读/加密/状态/排序、按名称读取、初始化 seed；审计/回滚/批量导入导出可延后。
- 已完成需求 `294962874827149219`（[nquiz迁移] ExamHistoryManager）分析并回写：基于 `quiz` 真实代码确认该页本质是“当前登录用户自己的考试历史与答卷复盘”，不是考试后台总览页。
- 关键证据：`quiz/frontend/src/pages/Exam/History/index.tsx`、`quiz/frontend/src/pages/Exam/History/Detail/index.tsx`、`quiz/frontend/src/pages/Exam/Take/index.tsx`、`quiz/frontend/src/router/index.tsx`、`quiz/backend/src/main/java/com/ck/quiz/exam/controller/ExamController.java`、`quiz/backend/src/main/java/com/ck/quiz/exam/service/impl/ExamServiceImpl.java`。
- 本次分析结论：`nquiz` 应按 App Router 拆成 `/exam/history` 与 `/exam/history/[resultId]` 两个路由，首批聚焦“我的历史列表 + 详情复盘 + 只看错题 + 题号导航 + 删除本人答卷 + 提交后直达结果”闭环；旧页面中前端存在但后端未支持的名称/分类树筛选不应原样照搬，详情接口缺少本人校验的问题必须在 nquiz 中修复。
- 已完成需求 `294962874827149246`（[nquiz迁移] GroupManagement）分析并回写：基于 `quiz` 真实代码确认该页不是孤立字典 CRUD，而是多个业务模块共用的“通用分组基础设施”入口。
- 关键证据：`quiz/frontend/src/pages/Group/index.tsx`、`quiz/frontend/src/pages/Group/api/index.ts`、`quiz/frontend/src/router/index.tsx`、`quiz/backend/src/main/java/com/ck/quiz/group/controller/GroupController.java`、`quiz/backend/src/main/java/com/ck/quiz/group/service/impl/GroupServiceImpl.java`、`quiz/backend/src/main/java/com/ck/quiz/group/entity/Group.java`、`quiz/backend/src/main/java/com/ck/quiz/group_obj/entity/GroupObjRela.java`、`quiz/backend/src/main/java/com/ck/quiz/base/service/impl/BaseServiceImpl.java`、`quiz/frontend/src/pages/MindMap/components/GroupTree.tsx`、`quiz/backend/src/main/java/com/ck/quiz/mindmap/service/impl/MindMapServiceImpl.java`、`quiz/backend/src/main/java/com/ck/quiz/mermaids/service/impl/MermaidDiagramServiceImpl.java`。
- 本次分析结论：`nquiz` 应把该能力重构为 `/groups` 分组中心页，并新增 `quiz_group` / `quiz_group_object_relation` 两张基础表，统一承接用户级分组 CRUD、按 type 查询 options、对象挂组能力；旧版 `/check/name` 未纳入 `type` 的唯一性校验缺陷必须在 nquiz 中修正。
- 已完成需求 `294962874827149252`（[nquiz迁移] FuncDocManager）分析并回写：基于 `quiz` 真实代码确认该模块不是单纯文档列表，而是“docx 功能文档上传 -> 标题解析 -> 流程节点提取 -> 三级功能点沉淀 -> AI 生成流程/接口说明 -> 导出”的功能文档工作台。
- 关键证据：`quiz/frontend/src/pages/FuncDoc/index.tsx`、`quiz/frontend/src/pages/FuncDoc/Detail.tsx`、`quiz/frontend/src/pages/FuncDoc/Features.tsx`、`quiz/frontend/src/pages/FuncDoc/api/index.ts`、`quiz/frontend/src/router/index.tsx`、`quiz/backend/src/main/java/com/ck/quiz/funcdoc/controller/FuncDocController.java`、`quiz/backend/src/main/java/com/ck/quiz/funcdoc/service/impl/FuncFuncDocServiceImpl.java`、`quiz/backend/src/main/java/com/ck/quiz/funcdoc/entity/FuncDoc.java`、`quiz/backend/src/main/java/com/ck/quiz/funcdoc/entity/FuncDocHeading.java`、`quiz/backend/src/main/java/com/ck/quiz/funcdoc/entity/FuncDocProcessNode.java`、`quiz/backend/src/main/java/com/ck/quiz/funcdoc/entity/FunctionPoint.java`。
- 本次分析结论：`nquiz` 应按 `/func-docs`、`/func-docs/[docId]`、`/func-docs/[docId]/features` 三段式路由迁移，首版保留上传、标题树/流程节点、功能点树与三级功能点、AI 生成、标题/接口导出闭环；同时把旧系统同步上传解析改为异步解析任务，统一 `FuncDoc/funcDoc` 命名，改用 `quiz_` 前缀新表并补齐状态、错误信息、用户隔离与结构化 `infDetail`。

- 已完成需求 `294962874827149237`（[nquiz迁移] TodoManagement）分析并回写：基于 `quiz` 真实代码确认该页本质是“当前登录用户的个人待办管理”，不是多人协作项目看板。
- 关键证据：`quiz/frontend/src/pages/Todo/index.tsx`、`quiz/frontend/src/pages/Todo/api/index.ts`、`quiz/backend/src/main/java/com/ck/quiz/todo/entity/Todo.java`、`quiz/backend/src/main/java/com/ck/quiz/todo/service/impl/TodoServiceImpl.java`、`quiz/backend/src/main/java/com/ck/quiz/cron/service/TodoScheduleExpireService.java`、`quiz/backend/src/main/java/com/ck/quiz/cron/exec/TodoScheduleExpireScanTask.java`。
- 本次分析结论：nquiz 应优先迁移“个人 Todo 列表 + 新增编辑 + 完成删除 + 本人权限隔离 + 过期机制 + Calendar 预留联动”闭环；`initMindMap`、作业自动拆待办、统计面板接入可延后。
- 已识别主要风险：Calendar 模块未迁移时的联动落空、serverless 部署下自动过期任务实现方式、旧版 `initMindMap` 权限校验偏弱、Todo 与 Calendar 双向同步的幂等与补偿设计。
- 已完成需求 `294962874827149210`（[nquiz迁移] KnowledgeManagement）分析并回写：基于 `quiz` 真实代码确认该页本质是“个人知识点卡片管理 + 复习 + 题目联动 + AI 辅助”，不是知识集/RAG 工作台。
- 关键证据：`quiz/frontend/src/router/index.tsx`、`quiz/frontend/src/pages/Knowledge/index.tsx`、`quiz/frontend/src/pages/Knowledge/Review.tsx`、`quiz/frontend/src/pages/Knowledge/api/index.ts`、`quiz/backend/src/main/java/com/ck/quiz/knowledge/controller/KnowledgeController.java`、`quiz/backend/src/main/java/com/ck/quiz/knowledge/service/impl/KnowledgeServiceImpl.java`、`quiz/backend/src/main/java/com/ck/quiz/knowledge/entity/Knowledge.java`、`quiz/backend/src/main/java/com/ck/quiz/base/service/impl/ReviewBaseServiceImpl.java`、`quiz/backend/src/main/java/com/ck/quiz/question/repository/QuestionKnowledgeRepository.java`。
- 本次分析结论：nquiz 应将该菜单重构为 `/knowledge` 知识学习工作台，首批保留知识点 CRUD、学科/分类维度、SM-2 复习、归档/重置、关联题目、AI 生成题目并批量入库；知识集/知识来源不纳入本需求。
- 已识别主要风险：`knowledge` 与 `knowledge-set` 命名混淆、旧版删除时对关联题目校验不足、HTML 富文本展示安全、题目模块迁移依赖、复习评分档位和 AI SSE 稳定性。

## 需求开发
- 已完成需求 `294962874827149386`（[nquiz迁移] DatasourceManagement）首期真实开发并准备回写。
- 本次在 `nquiz` 中新增 `/datasource` 页面，按“数据接入中心”而非旧版单纯表格管理页重构。
- 已落地内容：
  - `src/app/datasource/page.tsx` 页面入口。
  - `src/features/datasource/` 下的页面壳、表单抽屉、schema explorer、query hooks、API client、Zod schema。
  - `src/app/api/datasources/**` Route Handlers 与 `src/lib/datasource/mock-store.ts`，支撑 CRUD、输入态连接校验、已保存连接测试、schema 列表、只读预览、确认采集缓存。
  - `src/lib/query/query-keys.ts` 扩展 datasource 域 query keys。
  - `src/components/home/hero.tsx` 增加 DatasourceManagement 入口，便于工程内直达。
- 与旧 quiz 的关键差异：前端显式区分“只读预览 schema”与“确认采集并缓存”，避免旧版查看即写库的副作用。
- 已验证项：对新增/修改文件执行 `npx eslint ...` 定向检查通过。
- 未执行项：按本次任务约束，未做 `next build`、未做额外回归测试、未接入真实后端/数据库。
- 后续待补：真实持久化、密码加密存储、结构导出（Excel/JSON）、AI 备注与表分类增强、与 DataQuery 的真实共享数据域。

## 需求开发
- 完成需求 `294962874827149348`《[nquiz迁移] WxAppManager》的首版真实开发并准备回写。
- 基于 `quiz/frontend/src/pages/WxApp/index.tsx` 与后端 `wechart` 模块，确认需保留的核心闭环：列表筛选/分页、新增、编辑、删除、查看绑定用户。
- 在 nquiz 新增 `src/app/wxapp/page.tsx` 与 `src/features/wxapp/*`：
  - `wx-app-manager-page.tsx`：App Router 页面，采用 Tailwind + RHF + Zod + TanStack Query + Framer Motion 组织交互；
  - `schema.ts`：统一筛选/创建/编辑表单约束，明确编辑时 `AppSecret` 留空表示不修改；
  - `types.ts`：统一前端 DTO 命名为 `appId/appName/appDescr/createDate/updateDate`；
  - `mock-service.ts`：提供本地 mock 资源管理数据层，覆盖列表、创建、编辑、删除、绑定用户查询，并用 localStorage 维持交互闭环。
- 在 `src/lib/query/query-keys.ts` 增加 `wxApps` query key，支持列表与用户缓存失效刷新。
- 页面层已补齐：状态 badge、统计卡片、URL search params 筛选、删除二次确认、用户弹层空态/加载态、增删改成功反馈。
- 本次未接真实后端 API，也未拆独立详情页/用户子页；首版重点是先形成可演示、可继续接 API 的前端迁移闭环。
- 验证边界：按要求未执行 `npm run build`，也未做额外回归测试；仅执行 `npx eslint src/features/wxapp/wx-app-manager-page.tsx` 与 `npm run lint` 做静态检查。
- 当前 lint 结果：无 error；全项目仍有既有 warning（`.agents/skills/shadcn-ui/examples/data-table.tsx` 的 react-hooks/incompatible-library、`src/features/datasource/components/datasource-management-page.tsx` 的 unused var），与本次 WxAppManager 开发无直接关系。

- 已完成需求 `294962874827149168`（[nquiz迁移] 题库统计页）首版真实开发并准备回写。
- 本次在 `nquiz` 中新增 `/statistics-center/question-bank` 页面与 `/api/statistics/themes/question-bank/dashboard` BFF mock 聚合接口，先形成“当前登录用户维度题库运营看板”的前端闭环。
- 已落地内容：
  - `src/app/statistics-center/question-bank/page.tsx` 页面入口；
  - `src/features/statistics/question-bank/` 下的 dashboard 类型、API client、看板页面组件；
  - `src/lib/statistics/question-bank/mock-store.ts` 聚合 mock 数据，补充 `meta` 统计口径与 insights；
  - `src/app/api/statistics/themes/question-bank/dashboard/route.ts` Route Handler；
  - `src/lib/query/query-keys.ts` 扩展 statistics 域 query key；
  - `src/components/home/hero.tsx` 增加题库统计页入口。
- 与旧 quiz 的关键差异：删除旧版“各学科知识点数量统计”的误标，首版只保留可解释的学科题目分布表达；同时补充空态、错误态、刷新入口与口径说明。
- 已验证项：定向执行 `npx eslint src/features/statistics/question-bank/components/question-bank-statistics-page.tsx src/features/statistics/question-bank/api/client.ts src/features/statistics/question-bank/types.ts src/lib/statistics/question-bank/mock-store.ts src/app/api/statistics/themes/question-bank/dashboard/route.ts src/app/statistics-center/question-bank/page.tsx src/components/home/hero.tsx src/lib/query/query-keys.ts` 通过。
- 未执行项：按本次任务约束，未执行 `npm run build`，未做额外回归测试，统计接口当前为 mock BFF，尚未接 quiz/nquiz 真实后端聚合服务。
- 已完成需求 `294962874827149159`（[nquiz迁移] 百度网盘页）首版真实开发并准备回写。
- 基于 `quiz` 真实代码与需求 descr，明确该模块在旧系统中的真实语义是“百度网盘接入预留位”，不是已接通的文件管理工作台；因此 nquiz 首版刻意不迁移伪目录树/伪文件列表。
- 已落地内容：
  - `src/app/admin/integrations/baidu-pan/page.tsx`：新增 App Router 页面入口。
  - `src/features/baidu-pan/baidu-pan-integration-page.tsx`：用 Tailwind + TanStack Query + Framer Motion 重构接入状态页，覆盖接入总览、配置检查、能力矩阵、授权/解绑操作、管理员边界说明。
  - `src/app/api/baidu-pan/auth/{status,authorize-url,unbind}/route.ts`：新增 BFF Route Handlers，统一返回真实“未接通”语义，不返回 mock 文件数据。
  - `src/app/open/baidu-pan/auth/callback/page.tsx`：新增 Next.js OAuth 回调预留页，显式说明当前尚未实现 code -> token / 绑定落库。
  - `src/lib/baidu-pan/{config,types}.ts`：抽出状态 DTO 与配置检查逻辑，继续保留 `client_id / client_secret / redirect_uri` 三项旧语义，并改为从环境变量读取。
  - `src/lib/query/query-keys.ts`、`src/components/home/hero.tsx`：补齐 query key 与首页入口。
- 本次与旧 quiz 的关键差异：旧页虽然画了完整文件工作区，但真实后端全部未接通；nquiz 首版显式降维成“接入状态页”，避免继续制造伪完成感。
- 验证边界：按任务要求未执行 `npm run build`、未做额外回归测试；仅做定向 lint/static check。

- 已完成需求 `294962874827149156`（[nquiz迁移] 价格监控页）首版真实开发并准备回写。
- 基于 `quiz` 真实代码与需求 descr，确认该模块核心语义是“当前用户维护监控商品 + 手动采集价格快照 + 阈值预警”，不是自动爬虫抓价平台。
- 已落地内容：
  - `src/app/price-monitor/page.tsx`：新增 App Router 页面入口。
  - `src/features/price-monitor/{types,schema,mock-service,price-monitor-page}.tsx`：完成类型、Zod 校验、localStorage mock 数据层、列表/详情工作台页面。
  - `src/lib/query/query-keys.ts`：补齐 `priceMonitor` 域 query keys，覆盖列表、详情、趋势、快照、规则、预警日志。
  - `src/components/home/hero.tsx`：补齐首页入口。
  - 顺手修复 `src/features/wxapp/wx-app-manager-page.tsx` 中 `setFilters` / `setDialogMode` 未定义导致的明显运行阻断点。
- 本次与旧 quiz 的关键差异：
  - 前端显式按“单商品单规则配置”建模，不再延续 quiz 数据库支持多条、UI 实际只消费第一条的语义分裂。
  - 删除商品时联动删除快照、规则、预警日志，避免潜在孤儿数据。
  - 趋势图首版使用 React SVG 轻量实现，不继续沿用命令式 ECharts 生命周期写法。
- 验证边界：按任务要求未执行 `npm run build`、未做额外回归测试；仅做源码落地与静态自检准备，后续如程凯明确要求再执行编译/构建检查。

- 已完成需求 `294962874827149171`（[nquiz迁移] 单词熟练度统计页）首版真实开发并已准备回写。
- 基于 `quiz` 真实代码与需求 descr，确认该模块本质是 Vocabulary 域里的“当前用户单词学习洞察页”，而不是必须保留的独立统计中心页面。
- 已落地内容：
  - `src/app/vocabulary/proficiency/page.tsx`：新增 App Router 路由入口，直接挂到 Vocabulary 域。
  - `src/features/statistics/vocabulary-proficiency/{types,api/client,components/vocabulary-proficiency-page}.tsx`：完成 dashboard 类型、前端请求层、学习洞察页面实现。
  - `src/app/api/statistics/themes/vocabulary-proficiency/dashboard/route.ts`：新增 BFF Route Handler。
  - `src/lib/statistics/vocabulary-proficiency/mock-store.ts`：补齐 mock 聚合数据、口径说明与洞察摘要。
  - `src/app/vocabulary/page.tsx`、`src/app/vocabulary/review/page.tsx`：补齐列表/今日复习占位入口，确保统计页闭环链接可达而非 404。
  - `src/lib/query/query-keys.ts`、`src/components/home/hero.tsx`：补齐 query key 与首页入口。
- 本次与旧 quiz 的关键差异：
  - 不恢复已下线的 `statistics-center` 壳，直接按真实业务语义放到 `/vocabulary/proficiency`。
  - 保留旧 4 个 KPI、2 个学习质量指标与 3 张核心图的统计口径，但改成更偏学习仪表盘的信息架构。
  - 显式增加 loading / error / empty 三态和“去今日复习 / 回到单词列表”的业务闭环。
  - 图表首版用轻量声明式条形可视化表达，不继续沿用旧版命令式 ECharts 生命周期写法。
- 已验证项：定向执行 `npx eslint src/app/vocabulary/page.tsx src/app/vocabulary/review/page.tsx src/features/statistics/vocabulary-proficiency/components/vocabulary-proficiency-page.tsx src/features/statistics/vocabulary-proficiency/api/client.ts src/features/statistics/vocabulary-proficiency/types.ts src/lib/statistics/vocabulary-proficiency/mock-store.ts src/app/api/statistics/themes/vocabulary-proficiency/dashboard/route.ts src/app/vocabulary/proficiency/page.tsx src/lib/query/query-keys.ts src/components/home/hero.tsx` 通过。
- 未执行项：按本次任务明确约束，未执行 `npm run build`、未做额外回归测试；统计数据当前仍为 mock BFF，Vocabulary 列表/复习页仅补占位入口，真实主链路待后续需求迁移。

- 已完成需求 `294962874827149153`（[nquiz迁移] 热搜页）首版真实开发并已准备回写。
- 基于 `quiz` 真实代码与需求 descr，确认该模块核心语义是“热搜浏览 + 当前用户关注主题管理”，不是热搜采集后台或全局主题库。
- 已落地内容：
  - `src/app/hot-search/page.tsx`：新增 App Router 页面入口。
  - `src/features/hot-search/{types,schema,mock-service,hot-search-page}.tsx`：完成类型、Zod 校验、本地 mock 数据层、URL 化筛选、分页列表、详情区、关注主题管理侧边 Sheet。
  - `src/lib/query/query-keys.ts`：补齐 `hotSearch` 域 query keys，覆盖列表、详情、主题列表。
  - `src/components/home/hero.tsx`：补齐首页入口。
- 本次与旧 quiz 的关键差异：
  - 把筛选条件放入 URL，刷新后可恢复检索状态。
  - 把详情展示从列表负载中拆出，为后续接真实 `/hot-search/{id}` 详情接口留边界。
  - 把关注主题管理从旧版单页堆叠状态改成独立侧边 Sheet，降低页面耦合。
- 验证边界：按任务要求未执行 `npm run build`、未做额外回归测试；仅完成源码落地与静态结构自检。

- 已完成需求 `294962874827149273`（[nquiz迁移] 编排管理页）首版真实开发并准备回写。
- 本次在 `nquiz` 中新增 `/orchestration` 工作台与 `/orchestration/[workflowId]` 编辑器，先形成“工作流台账 + 标准化画布 + 版本发布 + 手动运行反馈”的首版闭环。
- 已落地内容：
  - `src/app/orchestration/page.tsx`、`src/app/orchestration/[workflowId]/page.tsx`：新增 App Router 路由入口；
  - `src/features/orchestration/{types,schema,constants,mock-service,orchestration-management-page,workflow-editor-page}.tsx`：完成类型、Zod 校验、节点目录、localStorage mock store、工作台页面、编辑器页面；
  - `src/lib/query/query-keys.ts`、`src/components/home/hero.tsx`：补齐 orchestration query key 与首页入口。
- 本次与旧 quiz 的关键差异：
  - 不再保存 React Flow 原始节点结构，统一改成标准 graph schema：`nodes[{id,type,name,position,config}] + edges[{id,source,target}]`。
  - 首版不暴露旧系统里前后端能力不一致的 `condition/sql/log` 组合，节点能力明确收敛为 `start / knowledge / llm / skill / end` 并全部可被 mock 运行器执行。
  - 运行反馈不再只弹 toast，而是沉淀到运行记录里，包含 run summary 与 step trace。
- 已验证项：
  - `npx eslint src/app/orchestration/page.tsx src/app/orchestration/[workflowId]/page.tsx src/features/orchestration/types.ts src/features/orchestration/schema.ts src/features/orchestration/constants.ts src/features/orchestration/mock-service.ts src/features/orchestration/orchestration-management-page.tsx src/features/orchestration/workflow-editor-page.tsx src/lib/query/query-keys.ts src/components/home/hero.tsx` 通过。
  - `npm run build` 通过，产物中已包含 `/orchestration` 与 `/orchestration/[workflowId]` 路由。
- 未执行项：
  - 未接 quiz / nquiz 真实后端工作流引擎、真实节点执行器、真实运行日志链路；当前 workflow/version/run 三层数据仍由 localStorage mock store 承接。

- 已完成需求 `294962874827149270`（[nquiz迁移] MCP 工具管理页）首版真实开发并准备回写。
- 本次在 `nquiz` 中新增 `/mcp-tool` 页面，形成“已入库 MCP 工具治理台账”的首版闭环。
- 已落地内容：
  - `src/app/mcp-tool/page.tsx`：新增 App Router 入口并用 `Suspense` 包裹 URL 化筛选页；
  - `src/features/mcp-tool/{types,schema,mock-service,mcp-tool-management-page}.tsx`：完成类型、Zod 校验、localStorage mock 数据层、筛选列表、详情侧栏、CRUD 弹窗与 JSON 配置治理；
  - `src/lib/query/query-keys.ts`、`src/components/home/hero.tsx`：补齐 `mcpTools` query keys 与首页入口。
- 本次与旧 quiz 的关键差异：
  - 明确把职责收敛为“已入库工具治理”，不再把工具发现、导入、运行态观察混在同一层；
  - 不再暴露旧前端声明但后端未落地的 enable/disable/clone/metrics/runtime 假能力；
  - 保留 `schemaJson / strategyJson / visibilityJson` 字段语义，但首版增加 JSON 合法性校验与详情侧可读化摘要。
- 本次顺手修复了一个全局构建阻断：
  - `src/app/layout.tsx` 移除了 `next/font/google` 的 Geist / Geist Mono 远程字体依赖；
  - `src/app/globals.css` 改为本地系统字体栈，避免 `next build` 因无法访问 Google Fonts 而失败。
- 已验证项：
  - `npx eslint src/app/layout.tsx src/app/mcp-tool/page.tsx src/features/mcp-tool/mcp-tool-management-page.tsx src/features/mcp-tool/mock-service.ts src/features/mcp-tool/schema.ts src/features/mcp-tool/types.ts src/lib/query/query-keys.ts src/components/home/hero.tsx` 通过。
  - `npm run build` 通过，产物中已包含 `/mcp-tool` 路由。
- 未执行项：
  - 未接真实 MCP Tool / MCP Server 后端接口，当前台账数据由 localStorage mock store 承接；
  - 未迁移独立的 MCP Server 页面，因此当前页只通过说明文案强调“发现入口在 MCP Server”。

- 已完成需求 `294962874827149276`（[nquiz迁移] Agent 管理页）首版真实开发并准备回写。
- 本次在 `nquiz` 中新增 `/agent` 页面，形成“Agent 配置资产中心”的首版闭环。
- 已落地内容：
  - `src/app/agent/page.tsx`：新增 App Router 入口，并用 `Suspense` 包裹 URL 化筛选页；
  - `src/features/agent/{types,schema,mock-service,agent-management-page}.tsx`：完成类型、Zod 校验、Agent/PromptTemplate/LLMModel/AgentTool mock 数据层、列表工作台、详情侧栏、配置弹窗；
  - `src/features/mcp-tool/mock-service.ts`：补充 `listSelectableMcpTools()`，让 Agent 页复用 MCP Tool 治理页里的真实可消费工具集合；
  - `src/lib/query/query-keys.ts`、`src/components/home/hero.tsx`：补齐 `agents` query keys 与首页入口。
- 本次与旧 quiz 的关键差异：
  - 不再照搬旧版“大 Modal + Tabs” 平铺结构，而是重构为列表工作台 + 详情侧栏 + 分区配置弹窗；
  - 补齐工具排序能力，对齐 `AgentTool.priority` 语义；
  - 页面定位明确收敛为配置资产中心，不承担聊天运行、日志监控、Prompt/模型本体维护。
- 已验证项：
  - `npx eslint src/app/agent/page.tsx src/features/agent/types.ts src/features/agent/schema.ts src/features/agent/mock-service.ts src/features/agent/agent-management-page.tsx src/features/mcp-tool/mock-service.ts src/lib/query/query-keys.ts src/components/home/hero.tsx` 通过。
  - `npm run build` 通过，产物中已包含 `/agent` 路由。
- 未执行项：
  - 未接真实 Agent / PromptTemplate / LLMModel / MCP Tool 后端接口，当前引用链路由 mock 数据与已迁移的 MCP Tool mock store 承接；
  - 首版未开放 `AgentTool.config` 深度编辑，只先交付工具选择与排序。

- 已完成需求 `294962874827149267`（[nquiz迁移] MCP 服务器管理页）首版真实开发并准备回写。
- 本次在 `nquiz` 中新增 `/mcp-server` 页面，形成“Server 台账管理 + 健康检查 + 发现工具入口”的首版闭环。
- 已落地内容：
  - `src/app/mcp-server/page.tsx`：新增 App Router 入口，并用 `Suspense` 包裹 URL 化筛选页；
  - `src/features/mcp-server/{types,schema,mock-service,mcp-server-management-page}.tsx`：完成类型、Zod 校验、localStorage mock 数据层、列表工作台、详情侧栏、编辑弹窗；
  - `src/features/mcp-tool/mock-service.ts`：新增 `upsertDiscoveredMcpTools()`，让 Server 页发现工具后真实同步到 Tool 域；
  - `src/lib/query/query-keys.ts`、`src/components/home/hero.tsx`：补齐 `mcpServers` query keys 与首页入口。
- 本次与旧 quiz 的关键差异：
  - 不再沿用旧版“点击名称打开工具抽屉”的隐式发现交互，而是改成显式 `健康检查 / 发现工具` 操作按钮；
  - 详情侧栏只展示认证配置脱敏摘要，不再回显完整敏感信息；
  - 发现结果会同步更新 MCP Tool 域，并刷新 Agent 可选工具引用链路，形成跨模块闭环。
- 已验证项：
  - `npx eslint src/app/mcp-server/page.tsx src/features/mcp-server/types.ts src/features/mcp-server/schema.ts src/features/mcp-server/mock-service.ts src/features/mcp-server/mcp-server-management-page.tsx src/features/mcp-tool/mock-service.ts src/lib/query/query-keys.ts src/components/home/hero.tsx` 通过。
  - `npm run build` 通过，产物中已包含 `/mcp-server` 路由。
- 未执行项：
  - 未接真实 MCP Server 后端 SSE / JSON-RPC 握手链路，当前健康检查和发现工具结果由 mock 规则承接；
  - 首版未开放在线 `call-tool` 调试能力，只交付管理与发现闭环。

- 已完成需求 `294962874827149177`（[nquiz迁移] SubjectManagement）首版真实开发并准备回写。
- 基于 `quiz` 真实代码与需求 descr，确认该模块本质是“当前登录用户自己的学科主数据管理”，不是全局学科字典中心。
- 已落地内容：
  - `src/app/subjects/page.tsx`：新增 App Router 页面入口。
  - `src/features/subject/{types,schema,mock-service,subject-management-page}.tsx`：完成类型、Zod 校验、localStorage mock 数据层、列表/摘要/创建编辑/删除阻断工作台页面。
  - `src/lib/query/query-keys.ts`：补齐 `subjects` 域 query keys，覆盖列表与下游 options 复用。
  - `src/components/home/hero.tsx`：补齐首页入口。
  - `MEMORY.md`：补充 SubjectManagement 模块长期记忆。
- 本次与旧 quiz 的关键差异：
  - 将旧页搜索从前后端错位的 `name/label` 透传，收敛为一个明确匹配 `name/label/descr` 的统一关键词。
  - 首版主动展示 `knowledgeNum/questionNum` 聚合信息，不再把后端已有统计语义埋没掉。
  - 删除时若仍有知识点或题目沉淀则直接阻断，不做静默级联删除，避免破坏下游 Category / Knowledge / Question 语义。
  - 继续保留“英文名仅在当前用户域内唯一”的旧业务边界，不误改成全局唯一。
- 已验证项：定向执行 `npx eslint src/features/subject/subject-management-page.tsx src/features/subject/schema.ts src/features/subject/mock-service.ts src/features/subject/types.ts src/app/subjects/page.tsx src/lib/query/query-keys.ts src/components/home/hero.tsx` 通过。
- 未执行项：按任务要求，未执行 `npm run build`，未做额外回归测试，当前数据层仍为 localStorage mock 闭环，尚未接真实后端/数据库。

## 需求开发
- 已完成需求 `294962874827149255`（[nquiz迁移] 通知发送页）真实开发，并通过 `cm-nquiz-requirement-develop` 回写 `COMPLETED`。
- 本次在 `nquiz` 中新增 `/notification/send`，形成“通知发送配置 + 异步任务创建 + SSE 日志流 + 异常补偿联动”的首版闭环。
- 已落地内容：
  - `src/app/notification/send/page.tsx`：新增 App Router 页面入口。
  - `src/features/notifications/send/{types,schema,api/client,notification-send-page}.tsx`：完成通知发送域类型、RHF + Zod 表单约束、前端请求层、发送工作台页面。
  - `src/app/api/notification/{recipients,send}/route.ts` 与 `src/app/api/notification/jobs/[jobId]/logs/stream/route.ts`：新增接收人查询、发送任务创建、SSE 日志流 Route Handlers。
  - `src/lib/notifications/send/mock-store.ts`：补齐服务端 mock store，承接接收人可用性、任务创建、日志流推进、失败落异常日志。
  - `src/lib/notifications/exception-logs/mock-store.ts`、`src/features/notifications/exception-logs/{schema,types,exception-log-page}.tsx`：补齐 `BROWSER` 渠道与跨页失败日志联动。
  - `src/lib/query/query-keys.ts`、`src/components/home/hero.tsx`：补齐通知发送域 query keys 与首页入口。
- 本次与旧 quiz 的关键差异：
  - 旧页“留空 userIds = 全员发送”改为显式发送范围 + 风险确认，不再依赖隐式语义。
  - 发送结果从单纯 toast 升级为结构化任务列表 + 任务级日志面板；日志通过 SSE route 实时输出。
  - 渠道命名从“BROWSER/系统消息”混用收敛为“站内消息”统一表达，并保留底层枚举兼容。
  - 失败任务会写入异常日志 mock store，通知发送页与异常日志页形成补偿闭环。
- 已验证项：
  - `npx eslint src/app/notification/send/page.tsx src/features/notifications/send/notification-send-page.tsx src/features/notifications/send/api/client.ts src/features/notifications/send/schema.ts src/features/notifications/send/types.ts src/app/api/notification/recipients/route.ts src/app/api/notification/send/route.ts src/app/api/notification/jobs/[jobId]/logs/stream/route.ts src/lib/notifications/send/mock-store.ts src/lib/notifications/exception-logs/mock-store.ts src/features/notifications/exception-logs/schema.ts src/features/notifications/exception-logs/types.ts src/features/notifications/exception-logs/exception-log-page.tsx src/lib/query/query-keys.ts src/components/home/hero.tsx` 通过。
  - `npm run build` 通过，产物中已包含 `/notification/send`、`/api/notification/send`、`/api/notification/recipients`、`/api/notification/jobs/[jobId]/logs/stream`。

## 队列续跑
- 按 `nquiz-queue-dev` 规则，完成 `294962874827149255` 后已立即重新查询 `OPEN` 队列；当前仍有 `49` 条 `OPEN`。
- 已自动领取下一条需求 `294962874827149252`（[nquiz迁移] FuncDocManager）并置为 `IN_PROGRESS`。
- 已回写阶段进度 `30%`：完成边界梳理，确认首版按 `/func-docs`、`/func-docs/[docId]`、`/func-docs/[docId]/features` 三段式路由闭环推进。

## 技能调整
- 按用户最新口径，已修改 `skills/nquiz-queue-dev`：执行时不再优先续跑历史半成品 active requirement，而是每次直接从当前 `OPEN` 队列领取需求开发。
- 已更新 `skills/nquiz-queue-dev/SKILL.md`：
  - 删除“先检查并续跑 existing active requirement”流程；
  - 新增“每次执行先忽略历史 active，并使用唯一 `STATE_FILE`（`/tmp/cm-nquiz-queue-dev/state-<RUN_ID>.json`）作为本轮状态文件”；
  - `start/progress/complete` 示例命令补齐 `--state-file "${STATE_FILE}"`。
- 已更新 `skills/nquiz-queue-dev/scripts/pre_final_gate.py`：
  - 终止条件从“`activeRequirementId=null` 且 `OPEN=0`”调整为“只要 fresh `OPEN=0` 即可终止”；
  - 保留 `activeRequirementId` 仅做可观测信息，不再作为门禁阻断。
- 已更新 `skills/nquiz-queue-dev/agents/openai.yaml`，同步默认提示词为“忽略历史半成品 active，每次从 `OPEN` 队列取单”。
- 已验证：
  - `python3 -m py_compile skills/nquiz-queue-dev/scripts/pre_final_gate.py` 通过；
  - `python3 /root/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/nquiz-queue-dev` 返回 `Skill is valid!`。

## 并发支持
- 按用户要求“确保工作区支持并发”，已把 `skills/nquiz-queue-dev` 调整为并发安全模式：
  - 运行态改为每次生成唯一 `STATE_FILE`：`/tmp/cm-nquiz-queue-dev/state-<RUN_ID>.json`，不再使用固定共享状态文件；
  - `start` 领取入口改为 `python3 skills/nquiz-queue-dev/scripts/pick_open_with_lock.py --state-file "${STATE_FILE}"`；
  - 新增 `pick_open_with_lock.py`，使用 `/tmp/cm-nquiz-queue-dev/pick.lock` 做领取阶段串行化，减少并发抢单重复风险；
  - `pre_final_gate.py` 默认状态文件切到 `/tmp/cm-nquiz-queue-dev/state-default.json`，与队列命名空间保持一致。
- 本次校验：
  - `python3 -m py_compile skills/nquiz-queue-dev/scripts/pick_open_with_lock.py skills/nquiz-queue-dev/scripts/pre_final_gate.py` 通过；
  - `python3 skills/nquiz-queue-dev/scripts/pick_open_with_lock.py --help` 可正常输出；
  - `python3 /root/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/nquiz-queue-dev` 返回 `Skill is valid!`。

## 队列执行（并发安全模式）
- 本次以独立状态文件 `/tmp/cm-nquiz-queue-dev/state-20260411-221610-2-15696.json` 实际执行 `nquiz-queue-dev`。
- 已完成需求 `294962874827149264`（[nquiz迁移] 知识集管理页）并成功回写 `COMPLETED`。
- 本次落地：
  - 新增路由 `src/app/knowledge/sets/page.tsx`；
  - 新增模块 `src/features/knowledge-set/{types,schema,mock-service,knowledge-set-management-page}.tsx`；
  - 扩展 `src/lib/query/query-keys.ts` 的 `knowledgeSets` 键空间；
  - 更新 `src/components/home/hero.tsx` 新增知识集管理入口；
  - 修复并发构建竞态：`next.config.ts` 支持 `NEXT_DIST_DIR`，并以独立 distDir 通过构建门禁。
- 验证：
  - 定向 `eslint` 通过（知识集模块、query-keys、hero、todo 修复）；
  - `NEXT_DIST_DIR=.next-queue-20260411-221610-2-15696 npm run build` 通过；
  - `complete --build-command \"NEXT_DIST_DIR=.next-queue-20260411-221610-2-15696 npm run build\"` 通过并回写 `COMPLETED`。
- 队列续跑：
  - 完成后 fresh 查询 `OPEN=41`；
  - 已按规则领取下一条 `294962874827149219`（ExamHistoryManager）并置 `IN_PROGRESS`；
  - 已回写阶段进度 `10%`（边界复核完成）。
- 当前阻塞：
  - 仓库处于高并发写入状态，`changedFilesSinceStart` 持续包含大量非当前需求文件，基线漂移明显；继续推进下一条需求前需确认并发推进策略（是否串行窗口、或固定单分支写入集合）。
